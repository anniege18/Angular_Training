import { ColorByTextDirective } from './color-by-text.directive';

describe('ColorByTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorByTextDirective();
    expect(directive).toBeTruthy();
  });
});
